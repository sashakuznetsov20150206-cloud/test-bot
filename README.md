from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import Application, CommandHandler, CallbackQueryHandler, MessageHandler, filters, ContextTypes
TOKEN = '8716731362:AAGYD4nzoz81pi97T4U2xvW9b6lkdrt-NXw' async def start(update: Update, context: ContextTypes.DEFAULT_TYPE): keyboard = [[InlineKeyboardButton("📦 Каталог", callback_data='catalog')], [InlineKeyboardButton("🛒 Корзина", callback_data='cart')], [InlineKeyboardButton("📞 Связь с владельцем", callback_data='contact')]] reply_markup = InlineKeyboardMarkup(keyboard) await update.message.reply_text('Привет! CarMax - автозапчасти. Выбери:', reply_markup=reply_markup) async def button(update: Update, context: ContextTypes.DEFAULT_TYPE): query = update.callback_query await query.answer() if query.data == 'catalog': keyboard = [[InlineKeyboardButton("Двигатель 1 (ID:001)", callback_data='add_001')], [InlineKeyboardButton("Тормоза 2 (ID:002)", callback_data='add_002')], [InlineKeyboardButton("🔙 Назад", callback_data='main')]] reply_markup = InlineKeyboardMarkup(keyboard) await query.edit_message_text('Каталог:', reply_markup=reply_markup) elif query.data.startswith('add_'): item_id = query.data.split('_')[1] context.user_data['cart'] = context.user_data.get('cart', []) + [item_id] await query.edit_message_text(f'✅ Добавлен {item_id} в корзину! Введи номер для подтверждения (или /cart):') elif query.data == 'cart': cart = context.user_data.get('cart', []) text = '🛒 Корзина пуста!' if not cart else f'🛒 Корзина: {", ".join(cart)}' keyboard = [[InlineKeyboardButton("🔙 Главное", callback_data='main')]] reply_markup = InlineKeyboardMarkup(keyboard) await query.edit_message_text(text, reply_markup=reply_markup) elif query.data == 'contact': await query.edit_message_text('Перейди в ЛС: и напиши заказ!') elif query.data == 'main': keyboard = [[InlineKeyboardButton("📦 Каталог", callback_data='catalog')], [InlineKeyboardButton("🛒 Корзина", callback_data='cart')], [InlineKeyboardButton("📞 Связь", callback_data='contact')]] reply_markup = InlineKeyboardMarkup(keyboard) await query.edit_message_text('Главное меню:', reply_markup=reply_markup) def main(): app = Application.builder().token(TOKEN).build() app.add_handler(CommandHandler("start", start)) app.add_handler(CallbackQueryHandler(button)) app.run_polling() if name == '__main__': main()
